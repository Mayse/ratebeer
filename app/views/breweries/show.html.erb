<p id="notice"><%= notice %></p>
<p><h2>
  <%= @brewery.name %>
</p>
<% if not @brewery.active  %>
	<span class="label label-info">retired</span>
<% end %>
</h2>
<p> <i>Established at <%= @brewery.year %></i> </p>
  <p>Number of beers <%= @brewery.beers.count%></p>
  <p>
   <% @brewery.beers.each do |beer| %>
	   <%= link_to beer.name, beer %>
	       <% end %>
	       </p>
<p> 
<% if @brewery.ratings.empty? %> No beer has ratings!  
<% else %> Has <%= @brewery.ratings.count %> <%= 'rating'.pluralize(@brewery.ratings.count) %>, average <%= round(@brewery.average_rating) %> 
<% end %> 
</p>


<%= link_to 'Edit', edit_brewery_path(@brewery), class:'btn btn-primary' %>
